{
  "$schema": "http://json-schema.org/draft-04/schema#",

  "title": "Tasks.Saved.v1",
  "description": "json schema for saving tasks (version 1)",

  "definitions": {
    "event_data": {
      "type": "object",
      "properties": {
        "public_id": {
          "type": "string"
        },
        "description": {
          "type": ["string", "null"]
        },
        "status": {
          "type": "string"
        },
        "account_id": {
          "type": "string"
        },
        "jira_id": {
          "type": ["string", "null"]
        }
      },
      "required": [
        "public_id",
        "status",
        "account_id"
      ]
    }
  },

  "type": "object",

  "properties": {
    "event_id":      { "type": "string" },
    "event_version": { "enum": [1] },
    "event_name":    { "enum": ["TaskSaved"] },
    "event_time":    { "type": "number" },
    "producer":      { "type": "string" },

    "data": { "$ref": "#/definitions/event_data" }
  },

  "required": [
    "event_id",
    "event_version",
    "event_name",
    "event_time",
    "producer",
    "data"
  ]
}
